import UserService from './UserService';

describe('User', () => {
    it('should to be able to create a new user', () => {
        expect(1 + 2).toBe(3);
    })
    
    it('should not be able to create two user with the same email', () => {
        expect(1 + 2).toBe(3);
    })
})
